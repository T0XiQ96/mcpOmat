# /speckit.analyze — Konsistenz-Checks

- Non‑Goals tauchen nirgends als „muss“ auf (nur Platzhalter‑Vars).
- LED‑Cap 55 %: in Tests und Game‑Render nachweislich wirksam.
- Admin‑PIN: UI‑Timer genau 10 s, dann Re‑Lock.
- „LichtLoser“: genau ein Verlierer, Joker nur bei 5 Spielern.
- Idle‑Flow: versteckter Start‑Button, Aktivierung zeigt ihn zuverlässig.
- Tasks decken Mapping (12 Displays) & Sitzplatz‑Overlays ab.
