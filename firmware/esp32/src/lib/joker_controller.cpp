#include "joker_controller.h"

namespace cabinet
{
    void updateJokerEffect(const SessionState &state)
    {
        // TODO: drive LVGL and LED outputs according to Joker settings.
        (void)state;
    }
}
