# Common Sources

This directory contains shared implementation files used by both the ESP32 and Mega builds. Each source file should include headers from `firmware/common/include/` and remain Arduino Core compatible.
